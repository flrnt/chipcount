<h1>Here you can see all our festivals</h1>

<% if @festivals.blank? %>
  <p>We don't have any record yet!</p>
<% else %>
  <ul>
    <% @festivals.each do |festival| %>
      <li>
        <%= link_to festival.name, festival_path(festival) %>
      </li>
    <% end %>
  </ul>
<% end %>

